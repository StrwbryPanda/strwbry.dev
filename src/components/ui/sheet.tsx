import React from 'react';
import * as Dialog from '@radix-ui/react-dialog';

const Sheet = Dialog.Root;
const SheetTrigger = Dialog.Trigger;
const SheetContent = Dialog.Content;

export { Sheet, SheetTrigger, SheetContent }; 